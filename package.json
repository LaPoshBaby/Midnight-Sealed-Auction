{
  "name": "midnight-sealed-auction",
  "version": "1.0.0",
  "description": "Midnight Sealed-Bid Auction",
  "main": "index.js",
  "scripts": {
    "compile": "compact compile",
    "test": "npx tsx --test tests/auction.test.ts"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.16.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
